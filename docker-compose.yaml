version: '3.8'

services:
  fastapi:
    build:
      dockerfile: Dockerfile
    command: poetry run uvicorn app.main:app --host 0.0.0.0 --port 8002
    networks:
      - nginx
    env_file:
      - .env
    ports:
      - "8002:8002"