services:
  tg_parser:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8002:8002"
    volumes:
      - ./app:/app/app
    env_file:
      - .env
networks:
  default:
    name: tg_parser-network